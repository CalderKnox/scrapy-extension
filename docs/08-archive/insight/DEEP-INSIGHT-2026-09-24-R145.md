# DEEP-INSIGHT 2026-09-24 — R145 post-R144 maintenance audit

> Generated 2026-09-24 against `main` @ `bf8bf99` (build(deps-dev): bump pymongo 4.17.0 → 4.18.1, #161).
> Scope: the unaudited September 2026 maintenance line (`9c56ff0` time-wheel overdue ordering,
> `3d369d7` JSON float overflow, `6d0fcfa`/`85698dc` SQS pop validation, `91fc35b` RocketMQ topic
> setup, `19bd512` integration skip gate, `c140be8` Redis example / manifest, docs restructure
> `9238d21`/`5007d8f`/`b1562cd`, Dependabot bumps #159–#172) plus a fresh eight-dimension pass
> (D1 concurrency & lifecycle, D2 delivery semantics, D3 security & redaction, D4 settings &
> validation, D5 test health, D6 documentation, D7 packaging & CI, D8 public API & typing).
> Every finding below was reproduced at runtime against the current tree with mocked drivers;
> nothing was fixed in this round. Ledger IDs are proposed as `R145-Fn`.

---

## 1. Executive summary

| Metric | Value | Notes |
|---|---|---|
| Baseline gates | all green | `ruff format --check` 331 files, `ruff check` clean, `mypy --strict src` 103 files clean, `bandit` clean |
| Baseline suite | **8296 passed / 55 skipped** (41.9 s, `-n auto`) | 55 skips = benchmark opt-in + integration opt-in, all classified |
| Coverage | serial unshuffled run (`-p no:randomly --cov`): **94.49 % statements / 91.61 % branches** | `ci.yml` gates 95.0 / 91.0 in the xdist lane only and quotes 95.46 / 91.65; both local attempts to reproduce the exact xdist lane were aborted by R145-F5, so the xdist number is unmeasured here. The nightly serial lane does not gate coverage. |
| CI on HEAD | green (CI, CodeQL, Documentation quality) | |
| Candidate findings | 22 | 8 self-refuted after adversarial re-check |
| **Confirmed** | **14** | **P0 0 · P1 0 · P2 5 · P3 9** |
| Ledger duplicates | 0 | 6 are new instances of LANDED root classes (cited per row) |

**One-line verdict.** No data-loss or credential-leak defect in a bundled default path. The five P2s are
sibling-parity gaps left behind by narrowly scoped earlier fixes: a public `RabbitMQBackend.pop(timeout=inf)`
that never returns (Redis/SQS got the validator, RabbitMQ did not), Kafka accepting blank TLS-file paths and
silently applying no CA/mTLS (every other backend rejects blanks), the shared drain helper discarding a
recorded Ctrl-C/SIGTERM when its deadline expires, the free-text `key=value` redaction regex drifting from the
unified fragment registry, and a timing-dependent Pulsar pump test pair that crashes an xdist worker in the exact
CI coverage-lane command (2/2 local lane failures on a 4-CPU host; serial 0/8).

**Method note.** The eight dimension reviewers were launched in parallel but were all terminated by an
environment usage limit at 02:53 UTC; their partial machine-check artifacts (D6 defaults/import/env-var
tables, D7 wheel/sdist listings, D8 export inventory, D5 serial coverage JSON, two D1 probes) were harvested
and every dimension was then completed by the coordinator. Coverage of D1/D2 is therefore shallower than the
2026-08-23 fan-out; the "verified clean" lists in §5 enumerate exactly what was read or probed.

---

## 2. Baseline state

| Gate | Result |
|---|---|
| `uv run ruff format --check src tests conftest.py` | 331 files already formatted |
| `uv run ruff check` | All checks passed |
| `uv run mypy --strict src` | Success: no issues found in 103 source files |
| `uv run bandit -q -r src -c pyproject.toml` | exit 0 |
| `uv run pytest -n auto` (Python 3.10.21) | 8296 passed, 55 skipped, 12 warnings in 41.90 s |
| Warnings | 9 × `DeprecationWarning: invalid escape sequence` (see R145-F12), 3 × expected Redis `master_slave` FutureWarning |
| `uv build` (D7) | wheel 103 `.py` = 103 `src` modules, `py.typed` present, `docs/08-archive/**` excluded, no repo side effects |
| Wheel in a no-extras venv | `import scrapy_extension` ok; `scrapy_extension.RedisBackend` → documented install hint |

---

## 3. Confirmed findings (P2 → P3)

Columns: proposed ledger id · dimension · severity · location (current tree) · root-cause class · what happens ·
runtime evidence · suggested fix · ledger relation.

### P2

| id | dim | file:lines | root class | risk | evidence | fix | ledger |
|---|---|---|---|---|---|---|---|
| **R145-F1** | D2/D4 | `backends/rabbitmq.py:1490` (`_basic_get`: `deadline = monotonic() + timeout if timeout > 0 else None`) | `rabbitmq-nonfinite-pop-timeout-unbounded-poll-loop` | Stable-tier `RabbitMQBackend.pop()/pop_with_ack()` accept a non-finite timeout. `inf` yields an infinite deadline and the loop polls `process_data_events(time_limit=0.05)` forever while holding the generation lease; `nan`/negative silently degrade to non-blocking. Redis (`redis.py:175 _normalize_pop_timeout`) and SQS (`sqs.py:117`, Sept fix) validate; the strategy layer (`queue/strategies/base.py:31 normalize_queue_timeout`, whose docstring names Redis as the motivating case) protects only the `BackendQueue` path. Kafka raises (`int(inf)`), Pulsar raises a static `QueueError`, Mongo/ES ignore the argument. | mocked pika connection, empty queue, 2 s watchdog: `rabbitmq.pop(timeout=inf) -> HANG (still running after 2s watchdog)`; `nan -> returned None after 0.001s`; `neg -> returned None`; `redis/sqs _normalize_pop_timeout(inf|nan|neg) -> rejected` | Add `validator=` (shared normalizer) to RabbitMQ `pop`/`pop_with_ack` (and Pulsar for a typed `ValueError` instead of an opaque `QueueError`); hoist the three copies of the normalizer (redis, sqs, strategies/base) into one helper. | NEW (R144-F8/F9 are different unbounded-wait mechanisms) |
| **R145-F2** | D4/D3 | `settings/kafka.py:441-453` (ssl_* Fields, no blank validator), `:695` (cert/key both-or-neither uses `is None`); `backends/kafka.py:1155-1160` (`if snapshot.ssl_cafile:`) | `kafka-blank-tls-path-accepted-and-silently-dropped` | `KafkaSettings(security_protocol='SSL', ssl_cafile='')` and `ssl_certfile=''/ssl_keyfile=''` construct; `_build_common_config` applies material by truthiness, so `''` vanishes — the client security config carries no `ssl_cafile` (kafka-python falls back to the system trust store: intended private-CA pinning silently not applied) and no client cert (mTLS silently not configured). `'   '` is forwarded verbatim and fails at connect (late, loud). Realistic trigger: templated `SCRAPY_KAFKA_SSL_CAFILE=""`. Siblings reject `''` at settings time (Redis `ssl_cafile`, RabbitMQ `ssl_cafile`, ES `ca_certs`, Pulsar `tls_trust_certs_file`; MongoDB at connect, see F11). | `KafkaSettings(security_protocol='SSL', ssl_cafile='') ACCEPTED -> client security config TLS keys: {'ssl_check_hostname': True}`; `ssl_certfile='', ssl_keyfile='' ACCEPTED -> {'ssl_check_hostname': True}`; control `ssl_cafile='/etc/ca.pem' -> {'ssl_cafile': '/etc/ca.pem', ...}`; `RedisSettings/RabbitMQSettings/ElasticSearchSettings/PulsarSettings` with `'' -> REJECTED (ConfigurationError)` | strip-aware `field_validator` on the three `ssl_*` fields (reject `''`/whitespace when not `None`); make the cert/key pairing check strip-aware; optionally fail closed on blanks in `_build_common_config`. | new instance of R141-F6 (`confluent-mode-drops-validated-tls-material`, LANDED) via a different mechanism; R141-F5/F17 blank sweeps did not cover Kafka `ssl_*` |
| **R145-F3** | D1 | `utils/_drain.py:50-70` (`bounded_drain_wait`); callers `queue/queue.py` close drain, `dupefilter/dupefilter.py _wait_for_quiescence_locked` | `drain-deadline-drops-recorded-control-signal` | The helper records a `KeyboardInterrupt`/`SystemExit` raised inside `Condition.wait` and keeps draining ("never trapped … the exact signal is re-raised"), but when the deadline passes before `busy()` clears it raises `BackendOperationTimeout` and the recorded signal is discarded — neither re-raised nor chained. `BackendOperationTimeout` is an ordinary `Exception` that `BackendQueue.close()`/dupefilter treat as retryable, so an operator Ctrl-C (or SIGTERM → `SystemExit`) during a hung-backend close drain becomes a retryable timeout and the process does not stop. | `/tmp/d1/probe_bounded_drain_drops_control_signal.py`: `raised: BackendOperationTimeout - Backend operation timed out: probe-drain. __cause__ = None __context__ = None waits performed after the interrupt: 2 CONFIRMED: KeyboardInterrupt recorded during the drain was dropped` | On deadline expiry with `control_error is not None`, re-raise the scrubbed signal (attach the timeout as `__context__`/note) or `raise timeout from control_error`; audit `backends/_generation.py` for the same shape. | new instance of the R53–R76 BaseException-window class; helper introduced by R144-F23 (LANDED) |
| **R145-F4** | D3 | `exceptions/base.py:407-415` (`_redact_message` key=value alternation) vs `:54-124` (`SENSITIVE_NAME_FRAGMENTS`, `_is_sensitive_name`) | `kv-redaction-alternation-diverged-from-fragment-registry` | R144-F21 unified the sensitive-fragment lists, but the free-text `key=value` regex is a fourth hand-written alternation (`password\|pass\|pwd\|private[_-]?key\|secret\|token\|credential\|authorization\|api[_-]?key`) that omits registry members `access_key`, `secret_key`, `cookie` and requires the separator immediately after the fragment, so a fragment embedded in a longer key (`aws_secret_access_key=`, `secret_key=`) or a quoted key (`"password": "x"`, `{'sasl_plain_password': 'x'}` — any `repr(config)` echoed by a driver/plugin) is not redacted, while `_is_sensitive_name()` classifies every one of those keys as sensitive. Exposure profile identical to R141-F4 (P2): bundled backends emit static terminal messages, so the leak needs a plugin backend or direct `BackendConnectionError(text)`. | `BackendConnectionError(text)`: `aws_secret_access_key=hunter2ZZ leaks=True`; `secret_key=`, `SECRET_KEY: `, `access_key=`, `cookie=` `leaks=True`; `'sasl_plain_password': 'hunter2ZZ'`, `"password": "hunter2ZZ"`, `{'password': 'hunter2ZZ'}` `leaks=True`; `client_secret=`, `sasl_password=`, `password='x'`, `Password:x` → `***REDACTED***` | Derive the alternation from `EXCEPTION_NAME_FRAGMENTS`, allow an optional `[a-z0-9_-]*` tail and an optional closing quote before the separator (`["']?\s*[=:]`); add a registry-vs-regex contract test. | new instance of R144-F21 (`divergent-sensitive-fragment-lists`, LANDED); adjacent to R141-F4 |
| **R145-F5** | D5 | `tests/test_pulsar_receive_pump.py:415` and `:544` (first `pop(..., timeout=0)` after connect) vs `backends/pulsar.py:1436-1459,1474-1481,1514-1515,1555` | `first-poll-races-pump-thread-failure-observation` | Both tests inject a receive failure the pump worker hits immediately, then assert the *first* `pop(timeout=0)` returns `None`. `_receive` with `deadline is None` performs one check: if the worker already recorded `failed`/`control_error` the terminal error is raised on the caller thread (`pulsar.py:1555`), else `None`. Under CPU contention the worker wins: `test_disconnect_retires_unobserved_terminal_consumer_across_reconnect[Exclusive]` fails with `QueueError: Failed to pop Pulsar message.`; in `test_failed_consumer_close_fences_concurrent_replacement_across_disconnect[base-exception-*]` the injected `KeyboardInterrupt` is re-raised on the test thread outside any `pytest.raises`, pytest treats it as a user interrupt and the **xdist worker crashes** (`[gw1] node down: keyboard-interrupt`, `replacing crashed worker gw1`, lane exit 2). Product behaviour (surface the observed terminal error, preserve control signals across the worker boundary) is by design. | Exact CI coverage-lane command (`-m "not integration" -n auto --randomly-seed=1125147632 --cov=…`) on a 4-CPU host: run 1 (loaded) `2 failed, 6727 passed, xdist.dsession.Interrupted` (`[Exclusive]`, `[base-exception-Exclusive]`); run 2 (idle) `2 failed, 6070 passed, Interrupted` (`[exception-Exclusive]`, `[base-exception-Failover]`) — **2/2 lane failures**. Controls: unseeded `-n auto` without `--cov` passed (8296/55); serial runs 0/8 bad with and without `--cov`; loaded serial loop (6 busy loaders, 20 runs) `pass=15 fail=2 interrupted=3`, the interrupted runs aborting the whole session (`!!! KeyboardInterrupt: cancelled receive !!!` from `test_pulsar_receive_pump.py:393`). Amplifier is xdist worker contention plus tracing, not coverage alone. | Gate the injected failure behind an `Event` set after the first poll returns (or drive the pump deterministically); the `KeyboardInterrupt` variant must never let the signal reach pytest's main thread outside `pytest.raises`. | NEW (R141-F3 was a different harness misalignment; no ledger row for this file) |

### P3

| id | dim | file:lines | root class | risk | evidence | fix | ledger |
|---|---|---|---|---|---|---|---|
| **R145-F6** | D4 | `schedule/scheduler.py:887-910` (shared-manager descriptor branch); `spider/spider_mixin.py:1587-1681 get_queue`, `:1839 get_dupefilter` | `mixin-capability-mismatch-falls-back-to-default-type` | `BackendSpiderMixin` with `backend_type = MEMCACHED` (storage-only): `from_crawler` succeeds, `get_queue()` returns a `BackendQueue`, `get_dupefilter()` a `BackendDupeFilter`. The factory branch adopts the mixin's real descriptor only `if … has_capability(candidate, "queue")`; when it lacks the capability, `backend_type`/`backend_settings` silently stay at the settings-resolved default (redis), so ack/snapshot/consumer-scope decisions are made for the wrong backend and the error surfaces only at first use (`push -> NotImplementedError: Selected backend does not support requested operations`; `request_seen -> RuntimeError "Configured backend does not support set/duplicate filtering"`). `BackendScheduler.from_settings`/`BackendDupeFilter.from_settings` reject the same backend at factory time, and `current-architecture.md` promises "Unsupported capabilities fail during factory setup with `ConfigurationError`". | factories: `Selected memcached does not support the queue/set interface …` (ConfigurationError); mixin: `get_queue() -> BackendQueue`, `push -> NotImplementedError …`, `get_dupefilter() -> BackendDupeFilter` | Raise `ConfigurationError` in the shared-manager branch when the mixin descriptor lacks the required capability (mirror `resolve_backend_config` wording); same for the dupefilter factory on the mixin path. | NEW (R136-F1/R138-F4/R141-F19 covered other mixin paths) |
| **R145-F7** | D1 | `backends/connectors/_manager.py:2302, 2344-2345` (`_finalize_retirement`: `self._breaker.reset()` under `self._lock`); `backends/circuit_breaker.py:337-352` (`reset` → `_notify_pending`) | `monitor-breaker-callback-runs-under-manager-lock` | `CircuitBreaker` appends transitions under its lock and drains observers outside it (`circuit_breaker.py:313-315`), but manager retirement calls `reset()` while holding the manager lock, so `Monitor.on_breaker_state` (R144-F22 hook) executes under `manager._lock` whenever a non-CLOSED breaker is retired (closing a manager after an outage). A slow monitor stalls every peer needing the manager lock during teardown; a monitor that touches `manager.backend` (public accessor) from the hook self-deadlocks `close()`. | `/tmp/d1/probe_breaker_reset_under_manager_lock.py`: `manager._lock held during on_breaker_state: [True]`; `close() finished within 3s: False — CONFIRMED: close() self-deadlocked on manager._lock` | Capture the breaker under the lock and call `reset()` (or at least its observer drain) after the `with self._lock:` block. | NEW (R144-F22 added the hook; no row on its lock scope) |
| **R145-F8** | D6/D4 | `README.md:768` vs `backends/connectors/_config.py:442-463` + `connectors/_constants.py:118-123` (`_SAFE_BACKEND_SETTING_HINTS` = {`database`, `SCRAPY_MONGO_DATABASE`}); `docs/06-guides/user-guides/migration-guide.md:736-742` | `typo-suggestion-allowlist-two-entries-and-env-typos-silently-ignored` | (a) Flat Scrapy settings / nested JSON typos are rejected, but the "Did you mean" hint is emitted only when the close match is in a two-entry allowlist, so every realistic typo yields the bare `ConfigurationError('Unknown bundled backend setting.', setting_name='backend_settings')` — neither the offending key nor a hint, although README promises "raise `ConfigurationError` with a suggestion". Bundled model field names are program constants and could all be safelisted. (b) Unknown prefixed environment variables (`SCRAPY_REDIS_PROT`, `SCRAPY_REDIS_HOSTNAME`, `SCRAPY_RETRY_ATTEMPS`, `SCRAPY_BACKEND_TYP`, removed `SCRAPY_RABBITMQ_CLUSTER_NODE_TYPE`) are silently ignored by every settings model; the migration guide documents the asymmetry, the README fail-fast row does not. | env probe: 8/8 typoed env vars `SILENTLY ACCEPTED`; flat probe: `'Unknown bundled backend setting.'` with no suggestion for `prot`/`hostname`/`cluster_node_type`; `difflib` finds `port`/`host`/`password`/`namespace`/`sentinel_master_name`/`ssl_cafile`/`max_connections`, none in the allowlist | Extend `_SAFE_BACKEND_SETTING_HINTS` with every bundled `_model_field_names(...)`; add an unknown-prefixed-env-name check or document the asymmetry in README. | adjacent to R141-F15 (safelist drift) and R144-F20 (env source fence); NEW instance |
| **R145-F9** | D4 | `settings/elasticsearch.py:110` (`cloud_id` Field); `backends/elasticsearch.py:738` (`if not snapshot.cloud_id`) | `es-cloud-id-whitespace-late-opaque-failure` | `ElasticSearchSettings(mode='cloud', cloud_id='   ', api_key='k')` constructs; the connect-time guard uses truthiness so `'   '` reaches the SDK and surfaces as the static `BackendConnectionError: Failed to connect to Elasticsearch.` with no hint that `cloud_id` is blank. Siblings (`username`, `ca_certs`, `hosts` entries) reject blanks at settings time. `mode='standalone', cloud_id='abc'` is accepted and silently ignored (MongoDB rejects TLS-file settings when TLS is disabled — asymmetric). | `ElasticSearchSettings(mode='cloud', cloud_id='   ', api_key='k') ACCEPT`; connect probe → `BackendConnectionError`, SDK kwargs seen `cloud_id='   '` | strip-aware validator for `cloud_id` in CLOUD mode; reject (or document) `cloud_id` outside CLOUD mode. | NEW (R66 covered the ES cloud `ValueError` wrapping; R45 index/auth) |
| **R145-F10** | D4 | `settings/rabbitmq.py:270-281` (`ha_mode`/`ha_params`/`ha_sync_mode`); `backends/rabbitmq.py:509-511, 609-611, 958` | `rabbitmq-ha-params-sync-mode-dead-config` | `ha_params` and `ha_sync_mode` are copied into the connection snapshot and never read again (zero consumers beyond snapshot construction); `ha_mode` only gates the #34 "NOT applied via AMQP" warning. All three accept `''`/`'   '`. Field descriptions ("HA parameters (number of replicas or node names)", "HA sync mode (automatic or manual)") imply the client applies them. R141-F16 removed `cluster_node_type` for the same reason (R25-H precedent) but left these. | field matrix: `ha_mode`/`ha_params`/`ha_sync_mode` `''`/`'   '` ACCEPT; `rg ha_params\|ha_sync_mode backends/rabbitmq.py` → only 194-196, 509-511, 609-611 | Delete `ha_params`/`ha_sync_mode` with a tombstone comment; either delete `ha_mode` or reword its description to "documented intent only; not applied by this client"; sync CHANGELOG/README. | new instance of R141-F16 / R25-H dead-config class |
| **R145-F11** | D4 | `settings/mongodb.py:1023-1035, 848-860` vs `backends/mongodb.py:481-492` | `mongodb-tls-file-blank-rejected-at-connect-not-settings` | `MongoDBSettings(tls_enabled=True, tls_ca_file='')` constructs; the blank is rejected only when the backend captures its connection snapshot (`MongoDB tls_ca_file must be a non-empty string when set.`). Fail-fast discipline says settings layer (R141-F18 wrong-failure-layer class). Loud, so P3. | sibling table: `MongoDBSettings(tls_enabled=True, tls_ca_file='') ACCEPTED` | Hoist the strip-aware check into `validate_mongodb_transport_security` (`tls_intent` loop). | new instance of R141-F18 class |
| **R145-F12** | D5/D7 | `backends/_json_codec.py:1` (module docstring `\ `); `tests/test_backends.py:2695-2699` (docstring `\Z`); `pyproject.toml [tool.ruff.lint] select` | `invalid-escape-sequence-in-docstring-w605-unselected` | `compile()` emits `DeprecationWarning` (3.10/3.11) / `SyntaxWarning` (3.12+) "invalid escape sequence" at both sites — 9 of the 12 baseline pytest warnings (once per xdist worker + `<unknown>`), a future `SyntaxError`. `ruff check --select W605` finds both and autofixes, but `W` is not in `select`; `filterwarnings = error::UserWarning` does not cover `DeprecationWarning`/`SyntaxWarning`, so the warning policy is blind to it. | `ruff check --select W605 src tests -> Found 2 errors (2 fixable)`; `python -W error::DeprecationWarning` compile → `SyntaxError: invalid escape sequence '\ '` / `'\Z'`; whole-tree compile scan: exactly 2 files | Escape (`\\ `, `\\Z`) or raw docstrings; add `W605` (or `W`) to the ruff `select`. | NEW |
| **R145-F13** | D8 | subpackage `__all__` lists (e.g. `queue.strategies`, `storage.strategies`, `exceptions`, `backends.circuit_breaker`, `backends.connectors`, `utils.reactor`, `utils.identity`) vs `.github/STABILITY.md` tier tables | `subpackage-exports-without-stability-tier` | The D8 inventory (258 `__all__` names, 0 missing, 0 PEP 562 `__dir__` gaps) lists 102 names exported by a subpackage `__all__` that are absent from the root `__all__` and from STABILITY.md (94 in non-underscore modules, ≈60 distinct after re-export layers): the eight `*QueueStrategy` classes and `QueueStrategyRestoreError`, `Passthrough/BatchedStorageStrategy`, the eleven `exceptions.*_error_boundary`/`sanitize_*` seams (R144-F27), `CircuitBreaker`/`BreakerState`/`CircuitBreakerOpenError`/`wrap_*`, `capable_backends` and the `*_CAPABLE_BACKENDS` sets, `validate_key_name`/`KEY_NAME_PATTERN`, `defer_to_thread_*`. Per STABILITY ("a name's tier comes from this document") they sit in the "public but tier-less" limbo R141-F22 flagged for four connector names. | `/tmp/d8/export_inventory.out`: `TIER-LESS public names …: 102` | Assign tiers in STABILITY.md (Experimental for strategy classes/seams, Internal for `_`-module re-exports) or trim the subpackage `__all__` lists; add a contract test comparing subpackage exports with the tier tables. | new instance of R141-F22 (LANDED for 4 names) |
| **R145-F14** | D7 | `.github/workflows/docs.yml:20` (`actions/checkout@v4`) vs `ci.yml`/`codeql.yml` (`@v7`) | `workflow-action-version-drift` | Dependabot bumped `markdownlint-cli2-action` 20 → 24 in the same workflow (#159) but `checkout` stayed at `v4`; no open Dependabot PR covers it (`gh pr list`: #173 uv-build, #174 environment). Cosmetic, but the docs lane runs a three-major-versions-older checkout than the other lanes. | `rg -o 'uses: [^ ]+' .github/workflows/*.yml` | Bump to `actions/checkout@v7`. | NEW |

---

## 4. Self-refuted candidates

| Candidate | Why it is not a defect |
|---|---|
| `delay.py` shares the time-wheel overdue-ordering bug fixed in `9c56ff0` | `delay.py:587-604` restores by `original_deadline = remaining` and preserves serialized order for equal deadlines; the fix was time-wheel-specific (slot order). The new regression test fails on the pre-fix module (2/2 params) — verified by loading `9c56ff0^:time_wheel.py`. |
| JSON codec: `Decimal('Infinity')`, `1e309`, `NaN` literal, 5000-digit int, duplicate keys, deep nesting | All rejected (`ValueError`); a 100 kB nested-bracket payload raises `RecursionError`, which `BackendQueue._process_pop` catches via `except Exception` → acked as poison + `SerializationError`. One-way conversions (Decimal/UUID/Path/set/tuple → str/list) are documented. |
| Kafka CONFLUENT + blank `confluent_bootstrap_servers` | Rejected at settings time ("Kafka CONFLUENT mode requires a real Confluent Cloud endpoint"). |
| Env scalar grammar accepts `'  3 '`, `'0x10'`, `'1e1'`, `'٣'`, `'yes'`, `' True '` | All rejected; only canonical spellings (`'1'`) accepted. |
| Raw pydantic `ValidationError` escaping for wrong-typed values (`RedisSettings(password=123456)`) | Documented (`README.md:768`, `STABILITY.md:125`) and content is redacted (`input_value=None`); the JSON `SCRAPY_BACKEND_SETTINGS` path wraps into `ConfigurationError('Connection manager configuration is invalid.')` with no leak. |
| Mixin on a storage-only backend silently disables dedup | The dupefilter maps `NotImplementedError` to a loud `RuntimeError`; the observed "degrading" log came from the probe environment's connection failure, not the capability error. Only the failure layer is wrong (F6). |
| SQS `queue_name_prefix` accepts `''`/whitespace/newline | Default V2 naming digests the prefix; `legacy_v1` fails loudly at `CreateQueue`. |
| `codeql.yml:66 uses: actions/setup-example@v1` | Commented template boilerplate. |
| Unsalted SHA-256 registry key over credential-bearing settings (`_manager.py:1369`) | Never logged or exposed by a `__repr__`; in-memory only. Hardening idea (per-process salt), not a defect. |

---

## 5. Surfaces verified clean

- **D1** `utils/reactor.py` (`defer_to_thread_ordered`/`bounded_deferred`: pre-fired sources cancel timers, submission/callback failures still settle both views); `_finalize_retirement` owner/contender hand-off; `bounded_drain_wait` happy path.
- **D2** `BackendQueue.push`/`_push` prepared-route commit, literal-`True` durability gate, replacement-token fail-closed path, post-commit `delay`/`source` consumption; `_process_pop` poison handling; `_BoundQueueAckToken` single-outcome settle; strategy-layer `normalize_queue_timeout` at every strategy; codec symmetry for bytes/bytearray/datetime/date/-0.0/1e308 and marker-shaped user dicts.
- **D3** R141-F4 (schemeless userinfo), R141-F13 (empty secret needle), R144-F21 registry projection still in place; URL/`password=`/`token:`/header/Basic-Bearer redaction; `ConfigurationError.setting_value` redaction for password/url/token/secret/api_key/pwd/passwd/credentials/access_key/private_key; loopback classifier fails closed for `127.1`, `0177.0.0.1`, `0x7f.0.0.1`, `2130706433`, `::ffff:127.0.0.1`, `0.0.0.0`, `[::]`, `localhost%25`, `xn--localhost`, zone ids, IDNA dots; JSON `SCRAPY_BACKEND_SETTINGS` wrong-typed secrets → redacted `ConfigurationError`.
- **D4** field × probe matrix for all 10 settings models (blank/whitespace/newline on `str`, `''`/`'  '` on `SecretStr`, `-1/0/1e9/nan/inf/True` on numerics): every credential, identity, endpoint, index/collection and TLS-file field except the ones in F2/F9/F10/F11 rejects blanks; every numeric bound holds; list endpoint fields reject blank members (Redis sentinels/cluster nodes, Mongo members, Kafka brokers, RabbitMQ nodes, ES hosts); component factories (`BackendScheduler`/`BackendDupeFilter`/`BackendPipeline.from_settings`) gate capabilities with `ConfigurationError`; `SCRAPY_QUEUE_NAME_GENERATION` ∈ {v2, legacy_v1} validated (R141-F8 landed).
- **D5** 8296/55 under xdist and serial; all 55 skips classified (benchmark/integration opt-ins); `SCRAPY_TEST_FAIL_ON_UNEXPECTED_SKIP` gate active in the CI coverage lane; 23 `time.sleep` in 14 test files, 1 `xfail`; `9c56ff0` regression test proven red on pre-fix code; nightly serial lane checks pass/fail only (coverage floor is xdist-only by design).
- **D6** 16 fenced-block imports, 12 backticked and 5 quoted dotted paths resolve; 259 setting names known, 635 doc tokens checked (all 64 unresolved are `SCRAPY_TEST_*`/`SCRAPY_EXAMPLE_*`/prefix tokens); R141-F7/F9/F19/F23 doc fixes still in place (`BackendQueue.serializer`/`.spider` properties exist, `on_buffer_depth`/`on_delay_depth` listed, README `setup_backend()` guidance aligned).
- **D7** sdist/wheel build clean; 103 wheel modules = 103 src; `py.typed`; `docs/08-archive/**` excluded; `.orig` present is the REFUTED D7-1 uv_build behaviour; single audit waiver (PYSEC-2017-83, expires 2026-12-31, scrapy 2.19 outside range); `uv==0.11.29` CI pin; matrix 3.10–3.14 matches classifiers; no repo side effects from the build.
- **D8** `mypy --strict src` clean; 258 `__all__` names, 0 missing, 0 `__dir__` gaps, 0 structural issues; no-extras wheel import + lazy hint.

---

## 6. Proposed LEDGER rows (append to `LEDGER.md` when triaged; state `OPEN` until a fix wave lands)

```markdown
| R145 | F1 rabbitmq-nonfinite-pop-timeout-unbounded-poll-loop | backends/rabbitmq.py:1490 | rabbitmq-nonfinite-pop-timeout-unbounded-poll-loop | OPEN | P2. Stable-tier RabbitMQBackend.pop(timeout=inf) polls forever holding the generation lease (2 s watchdog repro); nan/negative silently non-blocking. Redis/SQS validate (`_normalize_pop_timeout`), strategy layer protects only BackendQueue. See DEEP-INSIGHT-2026-09-24-R145 §3. |
| R145 | F2 kafka-blank-tls-path-accepted-and-silently-dropped | settings/kafka.py:441-453,695; backends/kafka.py:1155-1160 | kafka-blank-tls-path-accepted-and-silently-dropped | OPEN | P2. ssl_cafile=''/ssl_certfile=''/ssl_keyfile='' accepted; `if snapshot.ssl_cafile:` drops them so no CA pin / no mTLS is applied silently; siblings reject blanks. New instance of R141-F6. |
| R145 | F3 drain-deadline-drops-recorded-control-signal | utils/_drain.py:50-70 | drain-deadline-drops-recorded-control-signal | OPEN | P2. bounded_drain_wait records KeyboardInterrupt/SystemExit but raises BackendOperationTimeout (unchained) when the deadline expires first; callers treat it as retryable, so Ctrl-C during a hung close drain is lost. R53–R76 class; helper from R144-F23. |
| R145 | F4 kv-redaction-alternation-diverged-from-fragment-registry | exceptions/base.py:407-415 | kv-redaction-alternation-diverged-from-fragment-registry | OPEN | P2. Free-text key=value regex omits access_key/secret_key/cookie and quoted-key shapes; aws_secret_access_key=, secret_key=, "password": "x", {'sasl_plain_password': 'x'} leak while _is_sensitive_name() flags them. New instance of R144-F21; adjacent R141-F4. |
| R145 | F5 first-poll-races-pump-thread-failure-observation | tests/test_pulsar_receive_pump.py:415,544 | first-poll-races-pump-thread-failure-observation | OPEN | P2. First pop(timeout=0) races the pump worker's injected failure; the exact CI coverage-lane command failed 2/2 locally (4 CPUs, loaded and idle) with an xdist worker crashed by the escaped KeyboardInterrupt; serial 0/8; loaded serial loop 25 % bad. Test-side; product by design. |
| R145 | F6 mixin-capability-mismatch-falls-back-to-default-type | schedule/scheduler.py:887-910; spider/spider_mixin.py:1587-1681,1839 | mixin-capability-mismatch-falls-back-to-default-type | OPEN | P3. Storage-only mixin backend hands out BackendQueue/BackendDupeFilter; capability error surfaces at first op (NotImplementedError/RuntimeError) instead of factory-time ConfigurationError; ack/snapshot decisions computed for the default type. |
| R145 | F7 monitor-breaker-callback-runs-under-manager-lock | backends/connectors/_manager.py:2302,2344-2345 | monitor-breaker-callback-runs-under-manager-lock | OPEN | P3. breaker.reset() under manager._lock drains on_breaker_state observers under the lock; reentrant monitor self-deadlocks close() (repro). |
| R145 | F8 typo-suggestion-allowlist-two-entries-and-env-typos-silently-ignored | backends/connectors/_config.py:442-463; connectors/_constants.py:118-123; README.md:768 | typo-suggestion-allowlist-two-entries-and-env-typos-silently-ignored | OPEN | P3. "Did you mean" suppressed for all but 2 names; unknown SCRAPY_<BACKEND>_* env vars silently ignored; README overstates. Adjacent R141-F15/R144-F20. |
| R145 | F9 es-cloud-id-whitespace-late-opaque-failure | settings/elasticsearch.py:110; backends/elasticsearch.py:738 | es-cloud-id-whitespace-late-opaque-failure | OPEN | P3. cloud_id='   ' in CLOUD mode accepted, fails at connect with the static message; cloud_id ignored in STANDALONE. |
| R145 | F10 rabbitmq-ha-params-sync-mode-dead-config | settings/rabbitmq.py:270-281; backends/rabbitmq.py:509-511,609-611 | rabbitmq-ha-params-sync-mode-dead-config | OPEN | P3. ha_params/ha_sync_mode never read past snapshot construction; descriptions imply application. New instance of R141-F16/R25-H. |
| R145 | F11 mongodb-tls-file-blank-rejected-at-connect-not-settings | settings/mongodb.py:848-860,1023-1035; backends/mongodb.py:481-492 | mongodb-tls-file-blank-rejected-at-connect-not-settings | OPEN | P3. tls_enabled=True + tls_ca_file='' accepted at settings, rejected at connect. R141-F18 class. |
| R145 | F12 invalid-escape-sequence-in-docstring-w605-unselected | backends/_json_codec.py:1; tests/test_backends.py:2695; pyproject.toml ruff select | invalid-escape-sequence-in-docstring-w605-unselected | OPEN | P3. Two invalid escapes emit compile-time warnings (9 of 12 baseline warnings; future SyntaxError); ruff W605 detects/fixes but W is not selected. |
| R145 | F13 subpackage-exports-without-stability-tier | subpackage __all__ lists vs .github/STABILITY.md | subpackage-exports-without-stability-tier | OPEN | P3. 102 subpackage-exported names (≈60 distinct public) have no STABILITY tier. New instance of R141-F22. |
| R145 | F14 workflow-action-version-drift | .github/workflows/docs.yml:20 | workflow-action-version-drift | OPEN | P3. docs.yml checkout@v4 vs @v7 elsewhere; no open Dependabot PR. |
```

---

## 7. Suggested fix grouping

| Group | Findings | Files | Verification |
|---|---|---|---|
| G1 timeout parity | F1 | `backends/rabbitmq.py`, `backends/pulsar.py`, shared normalizer | new RED tests `pop(timeout=inf|nan|-1)` for every queue backend; `tests/test_rabbitmq_backend.py`; full gates |
| G2 Kafka/ES/Mongo blank-material validators | F2, F9, F11 | `settings/kafka.py`, `settings/elasticsearch.py`, `settings/mongodb.py` (+ safelist sync per R141-F15) | `tests/test_settings_validation.py`, `tests/test_backend_settings_contracts.py`, new blank-material RED→GREEN cases; `tests/test_settings_message_safelist_contract.py` |
| G3 drain control signal | F3 | `utils/_drain.py` (+ `backends/_generation.py` audit) | `tests/test_lifecycle_drain_deadlines.py` + new deadline-after-interrupt case |
| G4 redaction registry | F4 | `exceptions/base.py` | `tests/test_redaction.py`, new registry-vs-regex contract test |
| G5 Pulsar pump test determinism | F5 | `tests/test_pulsar_receive_pump.py` | 20× loop under CPU load (`/tmp/r145/flake_pulsar_pump.sh` shape) → 0 bad runs |
| G6 mixin capability gate | F6 | `schedule/scheduler.py`, `dupefilter/dupefilter.py` factory, `spider/spider_mixin.py` | `tests/test_spider_mixin.py` new storage-only-backend case |
| G7 breaker reset lock scope | F7 | `backends/connectors/_manager.py` | `tests/test_get_breaker_lock_scope.py` extension |
| G8 config diagnostics | F8 | `connectors/_constants.py`, `connectors/_config.py`, README | `tests/test_backend_config_adapter.py` suggestion cases |
| G9 dead config | F10 | `settings/rabbitmq.py`, CHANGELOG, README | `tests/test_settings_validation.py` enumeration sync |
| G10 hygiene | F12, F14 | `_json_codec.py`, `tests/test_backends.py`, `pyproject.toml`, `docs.yml` | `ruff check --select W605`; full suite with `-W error::DeprecationWarning` on 3.12+ |
| G11 API tiers | F13 | `.github/STABILITY.md` (+ optional `__all__` trims) | export-vs-tier contract test |

Ordering hint: G5 first (restores deterministic CI lane), then G1–G4 (the P2s, file-disjoint, parallelizable),
then the rest.
